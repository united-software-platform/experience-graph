# Правила чтения документов

Правила обращения к документам через `Read`, `Grep`, `Glob`: гейт чтения перед каждым вызовом, жёсткий запрет каталогов и границы контекста задачи.

---

## Навигация

- [Таблица правил](#таблица-правил)
- [Гейт чтения](#гейт-чтения-read-001003)
- [Запрет каталогов](#запрет-каталогов-read-004)
- [Границы правила](#границы-правила-read-005007)
- [Строгий гейт для секретов](#строгий-гейт-для-секретов-read-008)
- [Пример](#пример)
- [Версионирование](#версионирование)

---

## Таблица правил

| ID | Правило |
|----|---------|
| READ-001 | Перед КАЖДЫМ вызовом `Read`/`Grep`/`Glob` пройти гейт: цель чтения (файл или каталог) должна быть указана явно — в запросе пользователя, правилах проекта или тексте исполняемого сценария команды либо сабрутины |
| READ-002 | Если путь не указан явно — СТОП: инструмент не вызывается, у пользователя запрашивается явное разрешение с обоснованием (какой документ, зачем, что именно требуется) |
| READ-003 | «Похоже, пригодится для задачи» не является прохождением гейта; критерий — явное указание пути, а не оценка полезности |
| READ-004 | Запрет чтения содержимого каталогов (включая `Read`, `Grep`, `Glob`): `trash/`, `src/process/archive/`, `.claude/commands/on-check/` |
| READ-005 | Сценарий команды или сабрутина даёт право читать только те пути, которые называет поимённо |
| READ-006 | Ссылка на артефакт как на источник методики не даёт права читать его выходные файлы и отчёты |
| READ-007 | Расположения выходных артефактов методологии (реестр STOR-кодов, `STOR-001`..`STOR-017`) — чужие результаты; читаются только через гейт по явному указанию |
| READ-008 | Файлы с секретами (`.env` и его варианты, `.ssh/`, `.gh/`, файлы учётных данных) проходят строгий гейт: читаются только по явному разрешению пользователя, выданному под конкретную задачу; упоминание пути в правилах, конфигурации или сценарии разрешением не является |

---

## Гейт чтения [READ-001..003]

**[READ-001]** Перед КАЖДЫМ вызовом `Read`, `Grep` или `Glob` сначала ответь на вопрос:

> **Путь указан явно?** — цель чтения (файл или каталог) поимённо названа в запросе пользователя, в правилах проекта или в тексте исполняемого сценария команды либо сабрутины (Subroutine, SUB).

**[READ-002]** Если ответ **НЕТ — СТОП.** Не вызывай инструмент. Запроси у пользователя **явное разрешение с обоснованием**: какой документ, зачем он нужен и что именно из него требуется. Дождись ответа. Без разрешения чтение не выполняется.

**[READ-003]** «Похоже, что пригодится для задачи» — это **НЕ** прохождение гейта. Критерий READ-001 требует явного указания пути, а не твоей оценки полезности. Вызывать `Read`/`Grep`/`Glob` без прохождения гейта запрещено.

Типичные нарушения — чтение по собственной инициативе:

- сгенерированные проектные артефакты и результаты предыдущих итераций;
- чужие выходные файлы и соседние артефакты дерева требований;
- содержимое расположений из реестра STOR-кодов, см. [READ-007](#границы-правила-read-005007).

> **Примечание:** правила проекта, подключённые в системный промпт (`CLAUDE.md`, `AGENTS.md` и перечисленные в них документы), считаются названными явно: их чтение предписано самим промптом.

---

## Запрет каталогов [READ-004]

**[READ-004]** Содержимое перечисленных каталогов не читается **никогда** — ни `Read`, ни `Grep`, ни `Glob`, независимо от гейта:

- `trash/` — удалённые из методологии материалы;
- `src/process/archive/` — архив методологии ([M-006](../constitution.md#m-006-архивация-устаревших-документов): архивный документ не используется как источник истины для новых решений);
- `.claude/commands/on-check/` — сценарии команд на проверке, не входящие в актуальную методологию.

Это жёсткий запрет: он не снимается ни явным указанием пути, ни ссылкой из сценария команды.

---

## Границы правила [READ-005..007]

**[READ-005]** Сценарий команды или сабрутина даёт право читать **только те пути, которые называет поимённо**. Если сценарий читает `A`, это не разрешает по своей инициативе открыть смежный `B`.

**[READ-006]** Ссылка на артефакт как на источник **методики** (например, «см. UC-001» или «см. SUB-DDC») отсылает к правилам и подходу документа, но **НЕ** даёт права читать его выходные файлы и отчёты.

**[READ-007]** Расположения выходных артефактов методологии — записи реестра STOR-кодов `STOR-001`..`STOR-017` в [`documentation-structure.md`](../documentation-structure.md) — содержат результаты применения методологии: дерево бизнес-артефактов, доменную модель, технические спецификации, протоколы сеансов и архивы релизов. Они читаются только через гейт [READ-001](#гейт-чтения-read-001003) — по явному указанию, а не по собственной инициативе.

Расположение сценариев-сабрутин (`STOR-018`) под READ-007 не подпадает: это артефакт самой методологии, а не её результат; чтение по-прежнему требует прохождения гейта [READ-001](#гейт-чтения-read-001003).

> **Примечание:** ссылка на расположение даётся STOR-кодом, а не путём — см. [SREF-001](./storage-reference-rules.md#обязательность-stor-ссылок).
>
> **Примечание:** правило согласовано с ограничениями на git-операции чтения — см. [git-rules.md](./git-rules.md).

---

## Строгий гейт для секретов [READ-008]

**[READ-008]** Файлы с секретами не открываются по обычному гейту [READ-001](#гейт-чтения-read-001003).
Для них действует **строгий гейт**: чтение выполняется только по явному разрешению пользователя,
выданному под конкретную задачу.

Под правило подпадают:

- `.env` и любые его варианты (`.env.local`, `.env.production`, `.env.*`);
- содержимое `.ssh/` и `.gh/` — приватные ключи, токены доступа к репозиторию;
- файлы учётных данных агента: `.credentials.json`, каталог профилей `.claude-accounts/`.

Разница с [READ-001](#гейт-чтения-read-001003) — в том, что закрывает гейт:

| Гейт | Что достаточно для чтения |
|------|---------------------------|
| Обычный [READ-001] | Путь назван явно: в запросе пользователя, правилах проекта или сценарии команды |
| Строгий [READ-008] | Только явное разрешение пользователя под текущую задачу |

Упоминание пути **не является** разрешением, даже если путь назван поимённо: ни `env_file: .env`
в `docker-compose.yml`, ни ссылка из README, ни строка в правилах проекта, ни собственный совет
агента «посмотреть, что в `.env`» строгий гейт не закрывают.

### Что делать вместо чтения

Задача почти всегда требует не значений, а состава ключей — его даёт шаблон:

- имена переменных и их назначение брать из `.env.example`, он не содержит секретов;
- нужное значение запрашивать у пользователя либо использовать через окружение процесса,
  не печатая его;
- проверять наличие переменной, а не её содержимое: `[ -n "$TOKEN" ]` вместо вывода `$TOKEN`.

> **Внимание:** прочитанный секрет попадает в контекст сеанса и в историю диалога целиком —
> отозвать его оттуда нельзя. Поэтому правило запрещает само чтение, а не только вывод значения.

**Если файл секретов всё же прочитан** — по разрешению пользователя или по ошибке — значения
не пересказываются в ответе, не пишутся в файлы проекта, не попадают в сообщение коммита
и не отправляются во внешние сервисы.

---

## Пример

Запрос пользователя: «поправь `src/process/glossary.md`».

| Действие | Гейт | Итог |
|----------|------|------|
| `Read src/process/glossary.md` | путь указан явно | пройден — читать можно |
| `Read` бизнес-сценария из дерева артефактов (`STOR-003`) «вдруг там контекст» | путь нигде не назван | НЕ пройден → СТОП, запросить разрешение |
| `Grep` по `src/process/archive/` | каталог под запретом READ-004 | запрещено безусловно |
| `Read .env`, чтобы узнать порт сервиса | файл секретов, строгий гейт READ-008 | НЕ пройден → взять состав ключей из `.env.example`, значение спросить |

---

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-08-04 | Запрос пользователя: создать правила чтения документов | Claude Code | Claude Opus 5 | Начальное создание: READ-001..007 — гейт чтения перед каждым вызовом `Read`/`Grep`/`Glob` (явное указание пути, иначе СТОП и запрос разрешения), жёсткий запрет каталогов `trash/`, `src/process/archive/`, `.claude/commands/on-check/`, границы правила (сценарий команды и сабрутина дают право читать только поимённо названные пути, ссылка на методику не даёт права читать выходные файлы, расположения выходных артефактов `STOR-001`..`STOR-017` — только через гейт) |
| 1.1.0 | 2026-08-20 | Запрос пользователя: не читать `.env`, только строгий гейт | Claude Code | Claude Opus 5 | Добавлено правило `READ-008` и раздел «Строгий гейт для секретов»: файлы секретов (`.env` и варианты, `.ssh/`, `.gh/`, `.credentials.json`, `.claude-accounts/`) читаются только по явному разрешению пользователя под конкретную задачу. Зафиксировано отличие от `READ-001`: упоминание пути — в `docker-compose.yml`, README или правилах проекта — строгий гейт не закрывает. Описан порядок работы без чтения (состав ключей из `.env.example`, значение — у пользователя, проверка наличия вместо вывода) и запрет на пересказ прочитанного секрета в ответе, файлах проекта, сообщении коммита и внешних сервисах. В таблицу примеров добавлена строка про `Read .env` |
